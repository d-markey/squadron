import 'package:squadron/squadron.dart';

const concurrency_222 = ConcurrencySettings(
  minWorkers: 2,
  maxWorkers: 2,
  maxParallel: 2,
);

const concurrency_253 = ConcurrencySettings(
  minWorkers: 2,
  maxWorkers: 5,
  maxParallel: 3,
);

const delay_20ms = Duration(milliseconds: 20);

const delay_80ms = Duration(milliseconds: 80);
